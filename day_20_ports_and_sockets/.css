/* File: web/styles.css */

:root {
    font-family: Inter, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
    color: #e8eef7;
    background: #080b11;
}

* {
    box-sizing: border-box;
}

body {
    margin: 0;
    min-height: 100vh;
    background:
        radial-gradient(circle at top right, #16243a 0, transparent 32rem),
        #080b11;
}

.container {
    width: min(1400px, calc(100% - 32px));
    margin: 0 auto;
    padding: 48px 0;
}

.eyebrow {
    color: #68a8ff;
    font-size: 0.78rem;
    font-weight: 700;
    letter-spacing: 0.18em;
}

h1 {
    margin: 8px 0;
    font-size: clamp(2.5rem, 7vw, 5rem);
}

.description {
    max-width: 700px;
    color: #9ba8ba;
    line-height: 1.6;
}

.controls,
.status-panel {
    display: flex;
    flex-wrap: wrap;
    gap: 16px;
    align-items: end;
    margin: 32px 0 16px;
    padding: 18px;
    border: 1px solid #202b3b;
    border-radius: 14px;
    background: #0d121b;
}

label {
    display: flex;
    flex-direction: column;
    gap: 7px;
    color: #aab7c8;
    font-size: 0.85rem;
}

input,
select,
button {
    min-height: 40px;
    border: 1px solid #2a374b;
    border-radius: 8px;
    background: #111925;
    color: #e8eef7;
    padding: 0 12px;
}

input:focus,
select:focus,
button:focus {
    outline: 2px solid #68a8ff;
    outline-offset: 2px;
}

button {
    cursor: pointer;
    font-weight: 700;
}

.checkbox {
    flex-direction: row;
    align-items: center;
    padding-bottom: 10px;
}

.status-panel {
    justify-content: space-between;
    align-items: center;
    color: #aab7c8;
}

.table-wrapper {
    overflow-x: auto;
    border: 1px solid #202b3b;
    border-radius: 14px;
    background: #0d121b;
}

table {
    width: 100%;
    border-collapse: collapse;
    min-width: 900px;
}

th,
td {
    padding: 13px 14px;
    text-align: left;
    border-bottom: 1px solid #1c2634;
}

th {
    color: #8292a8;
    font-size: 0.75rem;
    text-transform: uppercase;
    letter-spacing: 0.08em;
}

td {
    color: #dbe5f2;
    font-family: "Cascadia Code", Consolas, monospace;
    font-size: 0.86rem;
}

tbody tr:hover {
    background: #121a26;
}

.error {
    color: #ff8f8f;
}
